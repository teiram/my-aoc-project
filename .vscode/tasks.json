{
    "version": "2.0.0",
    "tasks": [
                {
            "label": "sjasmplus",
            "type": "shell",
            "command": "/Applications/sjasmplus118", 
            "args": [
                "--lst=${fileDirname}/${fileBasenameNoExtension}.lst",
                "--sld=${fileDirname}/${fileBasenameNoExtension}.sld",
                "--raw=${fileDirname}/${fileBasenameNoExtension}.bin",
                "--fullpath",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
